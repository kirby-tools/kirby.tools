<template>
  <!-- A pixel out, so the rules sit beside the content column, never under it. -->
  <div
    aria-hidden="true"
    class="border-default absolute inset-x-4 inset-y-0 z-[-1] -mx-px hidden border-x sm:inset-x-6 lg:inset-x-8 lg:block"
  />
</template>
