---
title: Quick Reference
description: Keyboard shortcuts, configuration options, and supported field types at a glance.
---

A quick reference for Kirby Copilot's shortcuts, options, and capabilities.

## AI Providers

| Provider  | Configuration Key | Blocks/Layouts | Images     |
| --------- | ----------------- | -------------- | ---------- |
| OpenAI    | `openai`          | ✓ Good         | ✓ Yes      |
| Google    | `google`          | ⭐ Recommended | ✓ Yes      |
| Anthropic | `anthropic`       | ✓ Good         | ✓ Yes      |
| Mistral   | `mistral`         | ⚠️ Limited     | ⚠️ Partial |

## Default AI Models

Kirby Copilot uses sensible default models for both content generation and inline suggestions if none are specified.

| Provider  | Generation Model (`model`)   | Completion Model (`completionModel`) |
| --------- | ---------------------------- | ------------------------------------ |
| OpenAI    | `gpt-5.2`                    | `gpt-5-nano`                         |
| Google    | `gemini-3-pro-preview`       | `gemini-3-flash-preview`             |
| Anthropic | `claude-sonnet-4-5-20250929` | `claude-haiku-4-5-20251001`          |
| Mistral   | `mistral-large-latest`       | `mistral-small-latest`               |

::note
Google Gemini models are recommended for blocks and layouts due to their superior support for nested JSON schemas.
::

## Keyboard Shortcuts

| Action                                | Shortcut                                                                                        |
| ------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Open prompt (writer & textarea field) | :kbd{value="meta"} :kbd{value="."} (macOS) / :kbd{value="CTRL"} :kbd{value="."} (Windows/Linux) |
| Submit prompt                         | :kbd{value="meta"} :kbd{value="enter"} / :kbd{value="CTRL"} :kbd{value="enter"}                 |
| Cancel generation                     | :kbd{value="escape"}                                                                            |
| Trigger inline suggestion             | :kbd{value="meta"} :kbd{value=","} (macOS) / :kbd{value="CTRL"} :kbd{value=","} (Windows/Linux) |
| Accept inline suggestion              | :kbd{value="tab"} (Tab)                                                                         |
| Dismiss inline suggestion             | :kbd{value="escape"}                                                                            |

### Prompt History

Your prompts are saved in the browser's local storage (up to 50 entries per site). Access recent prompts via the history dropdown or navigate with arrow keys in the prompt textarea.

| Action          | Shortcut                                      |
| --------------- | --------------------------------------------- |
| Previous prompt | :kbd{value="↑"} (when cursor is at the start) |
| Next prompt     | :kbd{value="↓"} (when cursor is at the end)   |

## Placeholder Syntax

A placeholder is a field name wrapped in curly braces. When the prompt is sent, each placeholder is resolved with the actual field value from the current model (page, file, or site).

```
Summarize this article "{title}" in a meta description (max 155 characters). Include the main benefit and end with a subtle call-to-action. No quotes. This is the article:
{content}
```

::callout{icon="i-ri-information-line" color="info" to="/docs/copilot/prompt-dialog/placeholders"}
Learn more about field placeholders.
::

## Configuration Options

### Shared Options

These options are available for both the view button and Copilot section:

::field-group

::field{name="label" type="String" default="johannschopplich.copilot.label"}
Custom label for the view button or section. The default depends on the Panel language.
::

::field{name="userPrompt" type="String"}
Default user prompt that appears when the generation dialog opens. Users can edit the prompt before generating content.
::

::field{name="systemPrompt" type="String"}
Custom system prompt that controls how the AI structures and formats the generated content. Users cannot see or edit this prompt.
::

::field{name="icon" type="String" default="sparkling"}
Custom icon for the view button or section.
::

::field{name="theme" type="String" default="notice-icon"}
Controls the visual appearance and color theme of the button (e.g., `blue-icon`, `positive`, `negative-icon`).
::

::field{name="logLevel" type="String" default="warn"}
Set the logging level for debugging AI generation. Options: `error`, `warn`, `info`, `debug`.
::

::

### Section-Only Options

::field-group

::field{name="field" type="String" required}
Target field name for content generation.
::

::field{name="editable" type="Boolean" default="true"}
Allow prompt editing in the section.
::

::field{name="files" type="Boolean | String" default="true"}
File attachments configuration (`true`, `false`, `"auto"`).
::

::field{name="storage" type="Boolean" default="true"}
Persist prompts to local storage.
::

::field{name="open" type="Boolean" default="false"}
Initial accordion state.
::

::field{name="size" type="String" default="md"}
Button size.
::

::field{name="help" type="String"}
Help text displayed below the section.
::

::

## Supported Field Types

Kirby Copilot can generate content for all built-in Kirby field types, with the following exceptions:

- `pages`, `files`, `users` – Relation fields
- `gap`, `headline`, `info`, `line` – Blueprint fields without user input

## File Attachments

::card-group

::card{title="Images (JPEG, PNG, GIF, WebP)" icon="i-ri-file-image-line"}
Auto-resized to a maximum dimension of 2048 pixels.
::

::card{title="Images (JPEG, PNG, GIF, WebP)" icon="i-ri-file-pdf-line"}
Native parsing for files under 50MB; text extraction fallback for larger files.
::

::
